<Page
    x:Class="GitMC.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GitMC.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Margin="20">
        <StackPanel Margin="0,12,0,0" Spacing="12">
            <RichTextBlock>
                <Paragraph FontWeight="SemiBold" FontSize="36epx">Settings</Paragraph>
            </RichTextBlock>
            
            <InfoBar
                Title="Debug only!"
                Margin="0,8,0,0"
                IsClosable="False"
                IsOpen="True"
                Message="These actions are served as debug purposes only. You can use these to diagnose this application if translation process mets an error."
                Severity="Informational">
                <InfoBar.ActionButton>
                    <Button Content="Translation tool" Click="DebugToolsButton_Click" />
                </InfoBar.ActionButton>
            </InfoBar>
        </StackPanel>
    </StackPanel>
</Page>
